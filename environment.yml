name: langchain
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - ipykernel
  - pip:
      - langchain
      - langchain-community
      - langchain-core
      - langchain-openai
      - langgraph
      - langsmith
      - python-dotenv
      - pydantic
      - python-docx
      - pypdf
      - PyMuPDF
      - pywin32; platform_system == "Windows"
      - chromadb
      - minio
      - pydantic-settings
      - mcp
      - fastmcp
      - fastapi
      - uvicorn
      - typer
      - rich
      - pytest
      - pytest-asyncio
      - ruff
