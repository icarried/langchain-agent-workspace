interface:
  display_name: "OpenAI-compatible LLM Wrapper"
  short_description: "Add a worker to the unified OpenAI-compatible gateway."
  default_prompt: "Use $openai-compatible-llm-wrapper to add this agent as a streaming worker behind the unified gateway."
